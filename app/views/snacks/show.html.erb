<%= @snack.name %>
<%= @snack.price %>

Find this snack here:
<% @snack.machines.each do |machine| %>
  <li><%= machine.location %> (<%= machine.snacks.count %> kinds of snacks, average_price of <%= machine.avg_price %> )</li>
<% end %>
